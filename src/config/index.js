import routes, { externalRoutes, eduRoutes } from "./routes";
import { courses, registerLinks } from './courses';

const config = {
    routes,
    externalRoutes,
    eduRoutes,
    courses,
    registerLinks,
};

export default config;